# OSPF Routing

All routers run OSPF process `10` in Area `0`.

The supplied `show ip ospf neighbor` outputs show two `FULL` neighbors on every router. The supplied routing tables also show remote departmental networks with the `O` OSPF route code.

This provides dynamic routing across the three-floor topology and takes advantage of the triangular serial design.
